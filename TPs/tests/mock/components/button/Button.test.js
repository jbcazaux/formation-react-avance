describe('NiceDice', () => {
  const mock = null // TODO : 1 créer un mock

  it('renders DiceRoll', () => {
    // TODO : 2 snapshot
  })

  it('calls onclick', () => {
    // TODO : 3 vérifier que le mock est appelé lors du clic sur le bouton
  })
})
