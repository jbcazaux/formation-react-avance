import { roll } from './dice'

describe('dice', () => {
  beforeEach(() => {
    // TODO 1 : mock Math.random (renvoyer 0.5)
  })

  it('gets integer value', () => {
    const result = roll(6)

    // TODO 2 : le resultat doit etre 4
  })

  // TODO 3 : nettoyer les mocks
})
