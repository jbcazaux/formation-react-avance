import { roll } from './dice'

describe('dice', () => {
  beforeEach(() => {
    // TODO : 1 - mock Math.random
  })

  it('gets integer value', () => {
    const result = roll(6)

    // TODO : 2 - expect result to be 4
  })
})
